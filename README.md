# RNN-Sentiment-Analysis
RNN-based deep learning project for sequence prediction built using TensorFlow and deployed with Streamlit. The model learns patterns from time-series data to generate future predictions. Includes preprocessing, training, evaluation, and an interactive web interface for real-time prediction.
